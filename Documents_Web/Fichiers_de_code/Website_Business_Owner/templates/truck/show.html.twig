{% extends 'base.html.twig' %}

{% block title %}Truck{% endblock %}

{% block body %}
    <h1>Truck</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ truck.id }}</td>
            </tr>
            <tr>
                <th>Immatriculation</th>
                <td>{{ truck.registration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('truck_index') }}">Retour</a>

    <a href="{{ path('truck_edit', {'id': truck.id}) }}">Modifier</a>

    {{ include('truck/_delete_form.html.twig') }}
{% endblock %}
