{% extends 'base.html.twig' %}

{% block title %}Rank{% endblock %}

{% block body %}
    <h1>Rank</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rank.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ rank.title }}</td>
            </tr>
            <tr>
                <th>Minimum</th>
                <td>{{ rank.minimum }}</td>
            </tr>
            <tr>
                <th>Reward</th>
                <td>{{ rank.reward }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rank_index') }}">back to list</a>

    <a href="{{ path('rank_edit', {'id': rank.id}) }}">edit</a>

    {{ include('rank/_delete_form.html.twig') }}
{% endblock %}
