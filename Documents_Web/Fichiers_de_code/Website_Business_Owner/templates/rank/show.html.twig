{% extends 'base.html.twig' %}

{% block title %}Rank{% endblock %}

{% block body %}
    <h1>Rang</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ rank.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rank.title }}</td>
            </tr>
            <tr>
                <th>Minimum</th>
                <td>{{ rank.minimum }}</td>
            </tr>
            <tr>
                <th>Avantage</th>
                <td>-{{ rank.reward }}% sur commande</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rank_index') }}">Retour</a>

    <a href="{{ path('rank_edit', {'id': rank.id}) }}">Modifier</a>

    {{ include('rank/_delete_form.html.twig') }}
{% endblock %}
