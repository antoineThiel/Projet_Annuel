{% extends 'base.html.twig' %}

{% block title %}MenuToArticle{% endblock %}

{% block body %}
    <h1>MenuToArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu_to_article.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ menu_to_article.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('menu_to_article_index') }}">back to list</a>

    <a href="{{ path('menu_to_article_edit', {'id': menu_to_article.id}) }}">edit</a>

    {{ include('menu_to_article/_delete_form.html.twig') }}
{% endblock %}
