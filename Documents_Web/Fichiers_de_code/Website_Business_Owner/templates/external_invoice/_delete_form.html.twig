<form method="post" action="{{ path('external_invoice_delete', {'id': external_invoice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ external_invoice.id) }}">
    <button class="btn">Supprimer</button>
</form>
