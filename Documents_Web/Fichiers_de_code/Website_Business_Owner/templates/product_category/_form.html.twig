<div class="card card-primary card-outline">
    <div class="card-header">
        {% if(app.request.attributes.get('_route') == 'product_category_new') %}
            <h3>Create Category</h3>
        {% else %}
            <h3>Edit Category</h3>
        {% endif %}
    </div>
    <div class="card-body">
        {{ form_start(form) }}
        <div>
            {{ form_errors(form) }}
        </div>
        <div class="form-group">
            {{ form_row(form.name) }}
        </div>
        <button class="btn btn-primary">Add</button>
        {{ form_end(form) }}
    </div>
</div>