{% extends 'base.html.twig' %}

{% block title %}StockProduct{% endblock %}

{% block body %}
    <h1>StockProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stock_product.name }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ stock_product.unit }}</td>
            </tr>
            <tr>
                <th>Qty</th>
                <td>{{ stock_product.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stock_product_index') }}">back to list</a>

    <a href="{{ path('stock_product_edit', {'id': stock_product.id}) }}">edit</a>

    {{ include('stock_product/_delete_form.html.twig') }}
{% endblock %}
