{% extends 'base.html.twig' %}

{% block title %}MenuToDish index{% endblock %}

{% block body %}
    <h1>MenuToDish index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for menu_to_dish in menu_to_dishes %}
            <tr>
                <td>{{ menu_to_dish.id }}</td>
                <td>{{ menu_to_dish.quantity }}</td>
                <td>
                    <a href="{{ path('menu_to_dish_show', {'id': menu_to_dish.id}) }}">show</a>
                    <a href="{{ path('menu_to_dish_edit', {'id': menu_to_dish.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('menu_to_dish_new') }}">Create new</a>
{% endblock %}
