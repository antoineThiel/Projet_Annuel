<div class="card card-primary card-outline">
    <div class="card-header">
            {% if(app.request.attributes.get('_route') == 'product_origin_new') %}
                <h3>Créer Nouvelle Origine</h3>
            {% else %}
                <h3>Modifier Origine</h3>
            {% endif %}
    </div>
    <div class="card-body">
        {{ form_start(form) }}
        <div>
            {{ form_errors(form) }}
        </div>
        <div class="form-group">
            {{ form_row(form.country) }}
        </div>
        <button class="btn btn-primary">Enregistrer</button>
        {{ form_end(form) }}
    </div>
</div>
