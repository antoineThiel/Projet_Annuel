{% extends 'base.html.twig' %}

{% block title %}TruckComplaint{% endblock %}

{% block body %}
    <h1>TruckComplaint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ truck_complaint.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ truck_complaint.date ? truck_complaint.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ truck_complaint.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ truck_complaint.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('truck_complaint_index') }}">back to list</a>

    <a href="{{ path('truck_complaint_edit', {'id': truck_complaint.id}) }}">edit</a>

    {{ include('truck_complaint/_delete_form.html.twig') }}
{% endblock %}
