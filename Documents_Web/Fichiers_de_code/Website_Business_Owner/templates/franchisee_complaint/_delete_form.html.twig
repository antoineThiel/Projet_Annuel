<form method="post" action="{{ path('franchisee_complaint_delete', {'id': franchisee_complaint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ franchisee_complaint.id) }}">
    <button class="btn">Delete</button>
</form>
